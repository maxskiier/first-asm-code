MEMORY {
	RAM: start = $0100, size = $0700;
	ROM: start = $8000, size = $8000;
	ZP: start = $0000, size = $100;
}
SEGMENTS {
	ROM: load = ROM, type = ro;
	ZEROPAGE: load = ZP, type = zp;
	IRQROUTINE: load = ROM, type = ro;
	VECTORS: load = ROM, type = ro, start = $FFFA;
}
